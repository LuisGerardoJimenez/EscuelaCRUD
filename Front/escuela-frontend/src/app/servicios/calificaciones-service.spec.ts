import { CalificacionesService } from './calificaciones-service';

describe('CalificacionesService', () => {
  it('should create an instance', () => {
    expect(new CalificacionesService()).toBeTruthy();
  });
});
